# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## Release Strategy

**Note:** This project is currently in active development with frequent architectural changes and improvements. As such, no package releases will be made until the project reaches a mature and stable state. All changes are tracked in the `[Unreleased]` section below. Once the project stabilizes, versioned releases will begin following Semantic Versioning.

## [Unreleased]

### Added

- **Library — `financial_literacy` on analyze requests & stable imports**: Optional **`financial_literacy`** on **`AnalyzeInstrumentRequest`** / **`AnalyzeMarketRequest`** overrides the attached profile tier; **`copinance_os`** re-exports **`AnalyzeInstrumentRequest`**, **`AnalyzeMarketRequest`**, **`AnalyzeMode`**, **`RunJobResult`**, **`AnalysisReport`**, **`AnalysisProfile`**, and **`FinancialLiteracy`** for a single import path. Developer guide **`financial-literacy.mdx`** (nav **Financial literacy (clients)**); **Using as a Library** documents request-level literacy, precedence, and the intermediate default.
- **CLI — async commands**: Transient Rich **dots** spinner on **stderr** while **`async_command`** runs in an interactive terminal; suppressed for **`--json`** (machine-readable stdout) and non-TTY environments.
- **Transparency contract — options positioning + Greeks methodology (breaking)**: Methodology is now standardized through shared **`AnalysisMethodology`** / **`MethodologySpec`** value objects. `OptionsPositioningResult`, `AnalysisReport`, market-regime outputs, and backtest outputs all use the same structured envelope.
- **Financial literacy — options positioning copy**: `domain.literacy` (`TieredCopy`, `resolve_financial_literacy`, `financial_literacy_prompt_value`) and `data.literacy.options_positioning` tiered strings remain the narrative layer; aggregate positioning via `build_options_positioning` adapts signal names/explanations and `analyst_summary` for **beginner** / **intermediate** / **advanced**.
- **LLM — question-driven literacy output contract**: `literacy_output_contract_for_question_driven` in `domain.literacy` supplies tier-specific mandatory answer rules; `PromptManager` default kwargs inject `{literacy_output_contract}` (and `{current_date}`) from resolved `financial_literacy`; built-in `analyze_question_driven` system prompt leads with a literacy-first block. Unit tests cover contract text and shipped prompt substitution.
- **Options analytics modularization (breaking)**: Split monolithic modules into `data/analytics/options/greeks/` and `data/analytics/options/positioning/` with sub-computation modules and per-spec methodology. Public builder is now **`build_options_positioning`** returning `OptionsPositioningResult` directly (no legacy dict builder).
- **Options metadata cleanup (breaking)**: Removed ad-hoc `option_greeks_*` output keys from `OptionsChain.metadata`. Greeks methodology now lives in **`OptionsChain.greeks_methodology`** and positioning methodology in `OptionsPositioningResult.methodology`.
- **Documentation — options aggregate positioning**: README and CLAUDE plus docs site pages (CLI reference, quickstart, analysis modes, analysis reference index, market data tools, options & Greeks, architecture, library, API reference, options session, docs README, landing **index**).
- **CLI — `market options`**: Repeat **`-e` / `--expiration`** for multiple listed expiries (one table per expiry; **`--json`** emits **`multi_expiration`** with an **`expirations`** array when more than one date). Help text and user guide updated.
- **Options — multiple expirations**: **`AnalyzeInstrumentRequest`** accepts **`expiration_dates`** (list of YYYY-MM-DD) alongside optional **`expiration_date`**; values are merged and deduplicated (**`merge_instrument_expiration_inputs`** in **`domain/models/analysis.py`**). **`copinance analyze options`**: repeat **`-e` / `--expiration`** per date. Deterministic instrument analysis returns **`multi_expiration`** with per-expiry blocks when more than one date is requested; **`get_options_chain`** agent tool accepts **`expiration_dates`** and returns **`multi_expiration`** payloads for multiple chains. Docs (user guide, library, quickstart, analysis modes, options session, market data tools) and CLI command docstring updated.
- **Agent progress (library integrators)**: **`domain/models/agent_progress.py`** — versioned **`AgentProgressEvent`** union (`run_started`, `run_failed`, `run_completed`, `gathering_context`, `synthesis_phase`, `iteration_started`, `llm_stream`, `tool_started`, `tool_finished`) and **`parse_agent_progress_event`**. **`domain/ports/progress.py`** — **`ProgressSink`** protocol (exported from **`domain/ports/__init__.py`**). **`core/progress/`** — **`maybe_emit_progress`**, **`summarize_for_tool_args`**, **`summarize_tool_result`**.
- **Agent progress — compact timeline in results**: Question-driven success **`results`** may include **`agent_progress_timeline`** (JSON-serialisable event dicts; **`llm_stream`/`text_delta`** rows use **`text_delta_chars`** instead of raw tokens). **`RecordingProgressSink`** in **`core/progress/`**; opt out with **`include_agent_progress_timeline: false`** in **`run_job`** context. Developer guide and library docs updated.
- **Agent progress — wiring**: **`ToolExecutor`** emits **`tool_started`** / **`tool_finished`** when **`progress_sink`** and **`run_id`** are set. **OpenAI / Gemini / Ollama** **`generate_with_tools`** take **`progress_sink`** and **`run_id`** and emit **`IterationStartedEvent`** each iteration. **`QuestionDrivenAnalysisExecutor`** resolves **`run_id`**, emits lifecycle + **`llm_stream`** events (multiplexed from **`LLMTextStreamEvent`**), sets **`results["run_id"]`**; avoids default stdout streaming when only **`progress_sink`** is used. **`DefaultJobRunner`** binds **`run_id`** / **`job_execution_type`** in **structlog** `contextvars` when **`run_id`** is in job context. **`AnalyzeInstrumentRequest` / `AnalyzeMarketRequest`**: optional **`run_id`**; **CLI** passes **`run_id=None`** where required for typing. Unit tests: **`test_agent_progress`**.
- **Documentation — agent progress for clients**: **`docs/pages/developer-guide/agent-progress-client-integration.mdx`** (nav: **Agent progress & chat (clients)**); **Using as a Library** — **Gateway, chat UI, and agent progress** and link to that guide.
- **Architecture — use cases, tool bundles, and factories**: `domain/ports/use_cases.py` defines generic **`UseCase[TRequest, TResponse]`**; **`ToolBundleFactory`** and **`ToolBundleContext`** (`domain/ports/tool_bundles.py`, `domain/models/tool_bundle_context.py`) describe how agent tools are assembled from injected providers. Shared request/value types live in **`domain/models/market_requests.py`** and **`domain/models/analysis.py`**. **`AnalysisExecutorFactory`** moved to **`core/execution_engine/factory.py`** (from **`infra/factories/`**); **`LLMAnalyzerFactory`** added at **`ai/llm/analyzer_factory.py`**. **Pipeline tool discovery** (`core/pipeline/tools/discovery/`: specs, scan, loader; **`discovery/bundles.py`** delegates to market/regime/fundamental tool builders) composes the tool list without monolithic registry-only wiring.
- **Multi-turn question-driven analysis (library)**: `AnalyzeInstrumentRequest` and `AnalyzeMarketRequest` accept **`conversation_history`** (`list[LLMConversationTurn]`: alternating user/assistant, ending with assistant). The new user message is **`question`**. Prior turns are passed to **Gemini** as native `Content` turns + **systemInstruction**, and to **Ollama** via **`/api/chat`** `messages` — not concatenated into the shared prompt template. On success, executor results may include **`conversation_turns`** for the next call. **`parse_conversation_history`** (job context dicts) validates pairs. Default runners omit `conversation_history` from context when empty. **CLI:** single-turn only (no conversation flags); use the **library** (`conversation_history` on analyze requests or `run_job` context) for multi-turn.
- **LLM text streaming**: **`LLMProvider.generate_text_stream`** with **`LLMTextStreamEvent`** (Pydantic: **`text_delta`**, **`done`**, **`error`**, **`rollback`**), modes **`auto`** / **`native`** / **`buffered`**, **`LLMConfig.text_streaming_mode`**, and **`provider_config["disable_native_text_stream"]`**. Helpers **`ai/llm/tool_loop_streaming.py`**; native paths for **Gemini** (`generate_content_stream`) and **Ollama** (NDJSON **`/api/generate`**). Question-driven **`generate_with_tools`** accepts **`stream`** and **`on_stream_event`**; **`rollback`** when a streamed turn contained tool calls. Default stdout handler **`core/execution_engine/llm_stream_stdout.py`**; executor sets **`analysis_streamed`** and passes job **`context["stream"]`**. **CLI:** **`--stream`** on the **`analyze`** group (before **`equity` / `options` / `macro`**) and at **root** generic research (**`parse_root_argv`**); **`--json`** disables streaming. **`AnalyzeInstrumentRequest` / `AnalyzeMarketRequest`**: **`stream`**. **`DefaultAnalyze*Runner`**: forwards **`stream`** in context. Unit tests: argv dispatch, runners, workflows.
- **CLI — package layout**: Modular layout under `interfaces/cli/`: `commands/` (analyze, cache, market, profile, generic research), `shared/` (utils, formatting, error handling, profile context, market helpers, run-job JSON output), `dispatch/argv.py` (natural-language vs Typer routing), `main.py` (`main()` entry), `root.py` (Typer apps). Lazy **`app`** / **`version_app`** via `interfaces/cli/__init__.py` **`__getattr__`** for fast imports. **`[project.scripts]`** entry: **`copinance = "copinance_os.interfaces.cli:main"`**.
- **Question-driven execution — tool summary**: `core/execution_engine/question_driven_tool_summary.py` — **`is_tool_call_json_text`**, **`summarize_tool_calls_for_display`**, **`build_partial_synthesis_message`** for human-readable fallbacks when the final LLM text is missing or still looks like a tool call.
- **Analysis reports**: **`build_question_driven_analysis_report`** in **`domain/services/question_driven_analysis_report.py`** maps question-driven executor output to **`AnalysisReport`**; **`RunJobResult`** gains optional **`report_exclusion_reason`** (`ReportExclusionReason.UNKNOWN_EXECUTOR_TYPE`) when results exist but no report builder is registered for `execution_type`.
- **CLI — machine-readable output**: **`--json`** on the **`analyze`** and **`market`** Typer groups (before the subcommand) prints JSON to stdout: **`analyze`** emits **`RunJobResult.model_dump(mode="json")`** (including **`report`** and **`report_exclusion_reason`**); **`market`** emits a small envelope per subcommand (`command`, `response` / `rows` / `chain`, etc.). Helpers **`print_run_job_result_json`** and **`print_json_stdout`** in **`interfaces/cli/shared/utils.py`** (run-job wiring in **`shared/run_job_output.py`**).
- **Market data coercion**: **`CoerceMarketDataPointsResult`**, **`coerce_sorted_market_data_points_detailed`**, skip **warnings** with counts, and **`strict=True`** on **`coerce_sorted_market_data_points`** for fail-fast validation. Documentation in [API Reference — Data providers](https://copinance.github.io/copinance-os/developer-guide/api-reference#market-data-point-coercion) and User Guide CLI.
- **SEC / EDGAR**: **`edgartools`** dependency and **`EdgarToolsFundamentalProvider`** in `copinance_os.data.providers.sec.edgartools`; DI **`sec_filings_provider`** for question-driven agent tools (`get_sec_filings`, `get_sec_filing_content`). SEC identity via **`EDGAR_IDENTITY`** / **`COPINANCEOS_EDGAR_IDENTITY`** (defaults in settings). **Cache** integration with per-entry TTL via `CacheManager` to limit EDGAR traffic. **Pipeline tools** (registry + `fundamental_data.py`): company EDGAR profile, company facts statement, compare financials, XBRL statement table, insider Form 4, 13F institutional holdings, registered-fund search/entity/filings/portfolio/latest report — analytical tools ordered before generic filing list/content for LLM prompts.
- **Container/DI**: Optional `storage_type` and `storage_path` on `get_container()` for library integrators; pass `storage_type="memory"` to avoid creating a `.copinance` directory on disk without using `COPINANCEOS_STORAGE_TYPE`. Unit tests for container storage overrides.
- **Market search**: `EXCHANGE_DISPLAY_NAMES` and `format_exchange()` for human-readable market names (e.g. NMS → NASDAQ, NYQ → NYSE); "Market" column in search results; `longName`, `shortName`, and `exch_disp` from yfinance in results.
- **Container/DI**: Lazy container proxy so no container is created at import time; first `get_container()` or attribute access wins. Unit tests for container cache configuration.
- **CLI / logging**: `copinance` **`main()`** calls **`configure_logging(get_settings())`** at process start so **`COPINANCEOS_LOG_LEVEL`** / **`COPINANCEOS_LOG_FORMAT`** apply consistently. **`configure_logging`** routes stdlib logs to **stderr** via a handler that **follows `sys.stderr` each record** (compatible with Rich **`Live`/`Status`** proxies), raises noisy third-party loggers to **WARNING** when the root level is **DEBUG**, and uses a custom **structlog** console renderer for aligned **`[level]`** tags.
- **Documentation — logging**: Getting started **configuration** and **library** pages document logging env vars, CLI vs library embed startup, optional `.env` snippets, and clarifies that **`Settings`** can still read **`COPINANCEOS_*`** for knobs like logging when using **`get_settings()`** with **`get_container()`**. **README** adds a **VHS** command block for terminal demos.

### Changed

- **Financial literacy — defaults, job context, and macro cache**: **`AnalysisProfile`**, **`CreateProfileRequest`**, **`copinance profile create`**, and Typer-built analyze defaults use **`INTERMEDIATE`** (was beginner). **`DefaultAnalyzeInstrumentRunner`** / **`DefaultAnalyzeMarketRunner`** forward non-**`None`** request **`financial_literacy`** into job context; **`DefaultJobRunner`** no longer replaces an already-set request value with the profile. **`MacroRegimeIndicatorsTool`** stores **`_raw_interpretation`** in cached blocks and applies tiered **`interpretation`** strings per resolved literacy at read time (literacy-neutral cache entries).
- **Market regime (rule-based tools)**: Trend and volatility detector payloads now keep **canonical `regime` codes** for structured consumers and expose tiered copy as **`regime_label`** (literacy-aware), instead of overwriting **`regime`** with display strings.
- **Question-driven analysis**: System prompt stresses **one literacy tier at a time** (no mixed tone); beginner guidance defines unavoidable jargon on first use. Executor structlog context binds **`financial_literacy`** for observability.
- **Settings / DI — import and startup cost**: **`get_settings()`** returns a process-wide cached **`Settings`** instance (docstring notes how tests can reset). **`copinance_os.infra.di`** lazily resolves **`Container`** / **`get_container`** via **`__getattr__`** so importing the package does not pull in **`container.py`**. **`infra/di/container.py`**, **`data_providers.py`**, and market **`use_cases.py`** defer heavy vendor imports to **`configure_*`** bodies or **`_make_*`** factory helpers; profile providers live in **`infra/di/profile_use_cases.py`**. **`infra/logging.py`** defers **`structlog.dev`** imports until the aligned console renderer is built.
- **Options positioning input semantics (breaking)**: Aggregate positioning now treats missing contract Greeks/OI/volume as unavailable inputs (skipped in computations) rather than fallback zeroes; unavailable downstream fields stay explicit `null`.
- **Options positioning boundary validation**: `build_options_positioning` now raises domain `ValidationError` when no contracts/expiry are available for the requested positioning window instead of returning a silent empty payload.
- **Options positioning — expirations used for nearest selection**: **`sorted_expirations`** now returns unique ISO dates **only from option contract rows** (calls/puts), sorted with **`expiration_sort_key`**, instead of merging provider **`available_expirations`** metadata that may omit strikes for a listed date.
- **CLI — `market quote`**: Quote table **Volume** uses **`format_volume`** when a numeric value is present; **Market Cap** uses **`format_compact_number`** for human-readable scaling.
- **Documentation / README refresh**: Updated README and docs pages (CLI reference, market-data tools, options reference, library, examples, docs README) to document strict unavailable-value semantics (`null`), no-fallback behavior, and new validation-error behavior for empty positioning windows; replaced stale links to removed integration files.
- **CLI output contract docs**: Clarified machine-output/export semantics for `copinance analyze --json` and `analyze positioning` (stable `RunJobResult` keys, full `OptionsPositioningResult` under `results`, and explicit `null` for unavailable metrics).
- **Financial literacy personalization (full deterministic surface)**: Instrument summaries, market regime labels, macro interpretation labels, and question-driven/report fallback narration now resolve by `financial_literacy` tier (`beginner`/`intermediate`/`advanced`) across executors, tools, and report builders; unavailable values remain explicit (`null`) with no legacy fallback synthesis.
- **Options positioning methodology contract (breaking)**: Distributed methodology metadata to component-level sections (`signal_categories.*`, `iv_metrics`, `dollar_metrics`, `delta_exposure`, `vanna_exposure`, `charm_exposure`, `mispricing`, `moneyness_summary`, `pin_risk`, `implied_move_detail`) and reduced top-level `OptionsPositioningResult.methodology.specs` to cross-cutting specs only (Greeks when present, bias, data quality); removed legacy merge path and updated docs/fixtures/tests.
- **Methodology references — options analytics**: Corrected QuantLib/BSM and bias-literature citations with accurate titles, publication details, and stable DOI/source links.
- **Documentation — methodology terminology harmonization**: Standardized docs wording around the `AnalysisMethodology` envelope across options, reports, market-regime, CLI, and library guides. Terminology now consistently uses **methodology envelope**, **`specs`**, **`data_inputs`**, and **`greeks_methodology`** where applicable.
- **Package surface — library only**: Removed optional **`copinance_os.interfaces.api`** (FastAPI **`create_app`**, health/backtest/analyze SSE routes) and the **`api`** optional extra; removed **FastAPI** from **`[project.optional-dependencies] dev`**. Integrators use **`ResearchOrchestrator.run_job`** and **`ProgressSink`** from application code; see developer guide **Agent progress & chat integration (clients)**. **`interfaces/__init__.py`** docstring: CLI only.
- **Documentation**: **`docs/README`**, **installation** (drop **`pip install -e ".[api]"`**), **architecture** (package tree and table: no **`interfaces/api`**), **library** (gateway section, FastAPI references removed).
- **Research workflows & DI**: `research/workflows/` implementations (**`analyze`**, **`market`**, **`fundamentals`**, **`backtest`**, **`base`**) are thinner; concrete use cases are registered in **`infra/di/use_cases.py`** and **`container.py`**. **`core/orchestrator`**, **`runners`**, execution engines (**`instrument_analysis`**, **`market_analysis`**, **`question_driven_analysis`**), and **`core/pipeline/tools`** updated for the new contracts and tool assembly. **Data**: **`yfinance`**, **`edgartools`** adapter tweaks aligned with provider/tool usage.
- **Documentation**: Developer guide (**`architecture`**, **`extending`**), getting started (**`configuration`**, **`library`**), tools index (**`tools/index`**, **`market-regime`**, **`macro-indicators`**, **`data-providers`**), and API reference reflect use-case wiring, tool discovery, and layer boundaries. Root **`README`**, **`CONTRIBUTING`**, **`Makefile`**, **`docs/README`** adjusted for the same story.
- **Removed**: Empty placeholder packages **`ai/agents`**, **`ai/reasoning`**, **`ai/summarization`**, **`domain/portfolio`**, **`domain/risk`**, **`research/experiments`**, and the **`infra/factories/`** package (superseded by **`core/execution_engine/factory.py`** and **`ai/llm/analyzer_factory.py`**).
- **Tests**: New **`test_tool_discovery`**; **`test_yfinance`** refactored; factory/CLI/workflow tests updated for new import paths and wiring.
- **Tests**: **`test_get_quote_with_history`** mocks `hist["Volume"].fillna().sum()` so expected volume matches the session-total quote path (replacing a MagicMock that did not model the intraday series).
- **Documentation — `MANIFESTO.md`**: New principles—**AI explains, numbers trace to data**; **uncertainty named** (methodology and limits as analysis). Clarifies **Copinance OS** (engine/framework) vs **Copinance** (platform as one application), **three literacy tiers** (beginner / intermediate / advanced), bridge on the **five blockers**, and **open-source auditability** for financial tooling. Sections **Why I'm Building This** and **How I'm Building** promoted; **What I'm NOT Building** and **My Commitment** tightened; closing invites contributors. Punctuation and reference link cleanup.
- **Documentation — site Introduction (`docs/pages/index.mdx`)**: Landing page refocused on **conceptual clarity**—what the system is for, how layers differ, deterministic vs question-driven modes, orchestration, tools, and configuration—without leaning on class names on the intro; **Core concepts** expanded then rewritten in plain language; **Capabilities** and **What this system is** refreshed; codebase organization described as areas of responsibility with a pointer to the full package tree in Architecture.
- **Documentation — README (`Using the CLI`)**: Examples unified on the **`copinance`** command after install (`pip install -e .` / `make setup`); **Verify install** via `copinance version`; optional one-line note that `python -m copinance_os.interfaces.cli` is equivalent; consolidated command blocks for natural language, profiles, market, analysis, macro, JSON, streaming, cache, completion, and machine-readable output; removed duplicate `python -m` sample invocations.
- **CLI — streamed question-driven output**: **`render_run_job_results`** always prints the **Analysis** Rich panel (markdown / tables) when **`analysis`** is present, even if **`analysis_streamed`** is true, so users always get a readable copy-paste block after live tokens; dim hint explains live vs panel. User guide CLI updated accordingly.
- **CLI — help text for streaming**: Root **`typer.Typer` help** and **`_root_cli_epilog_natural_language`** (`--stream` example); lazy **`analyze`** command help mentions group **`--json`** / **`--stream`**; **`analyze_app` help** and **`--stream` Option** text; **`dispatch/argv.py`** docstring and stderr examples.
- **LLM providers (Gemini, Ollama)**: Question-driven tool-calling loops set **`synthesis_status`** (**`complete`** vs **`partial`**) and optional **`llm_synthesis_error`**; on partial completion (e.g. iteration error after tools ran, or final text still resembling JSON tool calls), substitute **`build_partial_synthesis_message`** so users see a deterministic tool summary instead of raw tool JSON.
- **CLI**: Removed monolithic **`interfaces/cli/*.py`** command modules at package root in favor of **`commands/`**, **`shared/`**, **`dispatch/`**, **`main.py`**, and **`root.py`**. Ruff **`PLC0415`** per-file ignores for lazy imports in **`interfaces/cli/__init__.py`** and **`interfaces/cli/main.py`**.
- **CLI / user guide**: Root `copinance --help` `market` summary aligned with the `market` Typer app (“options (BSM Greeks via QuantLib)”); `market options --expiration` Typer help aligned with the user guide default wording. CLI reference table (`market`, `cache`) and macro `--timeframe` default (`mid_term`) clarified.
- **Contributing**: `pip install -e ".[dev]"` only — removed nonexistent `.[dev,docs]` extra from the setup example.
- **Yahoo options:** Default `--expiration` now picks the earliest listed expiry **on or after today** (not raw `ticker.options[0]`), avoiding expired front months and missing BSM Greeks the day after expiry.
- **BSM / QuantLib:** Treat expiry **strictly before** evaluation as expired (`maturity < eval`); allow **same calendar day** as 0DTE. With normalized IV (see below), Greeks can compute for live expiries.
- **Yahoo options / BSM:** Normalize option-chain `impliedVolatility` when value is **> 1** (treat as percent points → divide by 100) so QuantLib BSM Greeks match σ as a fraction; fixes `-` Greeks when Yahoo returns values like `13.67` for ~13.7% vol.
- **CLI — `market options`:** Always shows Delta/Gamma/Theta/Vega/Rho columns by default (cells `-` when Greeks are not computed); `--no-greeks` hides them. Expanded help text and short flags for expiration/side. Docs (user guide, README, library API, options chain metadata) updated for BSM Greeks and `COPINANCEOS_OPTION_GREEKS_*` settings.
- **Documentation** (README, `CHANGELOG`, docs site, user guide, library/API reference): multi-turn question-driven analysis (`conversation_history`, provider-native chat history); root natural-language CLI (`copinance "…"`), `main`/`dispatch` vs lazy Typer, executor/`DefaultJobRunner`; `.venv` / `make setup`; storage vs cache and `storage_type="memory"`; JSON CLI output, structured reports, and market point coercion; package metadata and architecture/extension docs.
- **Market search**: Cache hits that are all "stub" instruments (e.g. bad symbol "APPLE") are treated as empty so the provider is queried and can return real results (e.g. AAPL). When symbol lookup fails, fall back to provider name search (yfinance Search) so queries like "APPLE" resolve to AAPL.
- **Container/DI**: `cache_enabled` and `cache_manager` overrides are applied when returning an existing container, so library callers (e.g. `get_container(cache_enabled=False)`) are no longer ignored.

### Fixed

- **Docs — library literacy example**: Replaced smart quotes in the **Using as a Library** Python snippet with ASCII string literals so the sample is valid Python.
- **yfinance quotes**: Intraday quote `volume` prefers a whole-session sum from 1m history when available (latest bar volume is often zero); `info` volume fields use safe integer coercion with fallbacks (`regularMarketVolume`, `volume`, `averageVolume`, `averageDailyVolume3Month`).
- **Options positioning tests**: Regenerated `tests/fixtures/options_positioning/toy_near.json` to match updated methodology reference metadata and keep golden-fixture regression assertions in sync.
- **Fundamentals use case**: `GetStockFundamentalsUseCase` rejects **`periods < 1`** with **`ValidationError`** (**`periods must be at least 1`**) after the workflow refactor briefly allowed invalid counts to reach the provider.
- **yfinance provider tests**: Updated unit and integration expectations to assert domain `DataProviderError` (instead of `ValueError`) for invalid-symbol and provider-failure paths, matching adapter exception contracts.
