"""Research experiments and backtests.

Use ``copinance_os.domain.backtest`` for deterministic long-only backtests; HTTP API
exposes the same engine under ``interfaces.api.create_app`` when FastAPI is installed.
"""
